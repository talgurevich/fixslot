MESSAGING_PROVIDER=fake
DASHBOARD_PASSWORD=changeme
TIMEZONE=Asia/Jerusalem
PORT=3000
DATABASE_URL="file:./dev.db"
SESSION_SECRET=dev-only-not-secret
# Real Green API credentials live only on the instructor's machine / deploy, never in the repo:
# MESSAGING_PROVIDER=greenapi
# GREEN_API_INSTANCE=
# GREEN_API_TOKEN=
